# Platform megjegyzés:
#   Az alábbi parancsok Windows-ra vannak írva (py launcher + backslash).
#   macOS / Linux alatt:
#     - "py"   helyett   "python3"
#     - "pip"  helyett   "pip3"
#     - "del"  helyett   "rm"
#     - backslash (\) helyett forward slash (/) az útvonalakban
#   Pl.: python3 split_srt.py "input/Sorozat - S01E01.eng.srt"

# 0. (OPCIONÁLIS) SDH-forrás előtisztítása — CSAK ha a felirat hang-/zene-
#    jegyzeteket tartalmaz ([music playing], [sighs], NÉV: címkék stb.)
#    Részletek: srt-preclean-addon\README.md
# py srt-preclean-addon\preclean_srt.py "input\Sorozat - S01E01.eng.srt"
# FONTOS: preclean után a 4. lépés ellenőrzését a .clean.srt ellen futtasd
# (az addon töröl cue-kat és ÚJRASZÁMOZ, ezért az eredeti .eng.srt-hez
# képest hamis hibákat jelezne), vagy használd az addon verify_preclean.py-át.

# 1. SRT szétvágás (ezt TE futtatod, nem kell hozzá Claude)
py split_srt.py "input\Sorozat - S01E01.eng.srt"
# Újra-splitnél (pl. más --block-size) a régi blokkok törléséhez: --clean
# (enélkül leáll, hogy a régi + új blokkok ne keveredjenek a merge-nél)

# 1.5 (OPCIONÁLIS, AJÁNLOTT) Glossary feltöltése MÉG A FORDÍTÁS ELŐTT
#     Csak az angol forrásból kinyeri a neveket / megszólításokat / helyszíneket,
#     az agent JAVASLATOT tesz a magyar fordításra (a TRANSLATION.md szabályai alapján),
#     te jóváhagyod (y/n/e/q). Így a párhuzamos fordítás már EGYSÉGES nevekkel indul.
#     (Csak az angol fájlt add meg — a magyar argumentum elhagyása kapcsolja be ezt a módot.)
py glossary_extract.py "input\Sorozat - S01E01.eng.srt"
# Codex providerrel:
# py glossary_extract.py "input\Sorozat - S01E01.eng.srt" --provider codex

# 2a. Fordítás Claude Code-dal (ez indítja a párhuzamos agent-eket)
py translate_parallel.py "blocks\Sorozat - S01E01.eng" --agents 3

# 2b. ALTERNATÍVA: fordítás Gemini API-val (olcsóbb, ugyanazt a blocks/ mappát fordítja)
#     A Gemini és Codex fordító ugyanúgy checkpoint-ol, ezért provider-váltással is folytatható.
#     Default modell: gemini-3.1-flash-lite
# py translate_with_gemini.py "blocks\Sorozat - S01E01.eng" --agents 3
# Opcionálisan:
#   --model gemini-3.1-flash      más Gemini modell
#   --block 3                     csak a 3. blokk újrafordítása (auto zero-pad)
#   --max-retries 6               több retry rate-limit/5xx esetén
# Előfeltétel: pip install google-genai python-dotenv pydantic
# Előfeltétel: .env fájl GEMINI_API_KEY=... bejegyzéssel

# 2c. ALTERNATÍVA: fordítás Codex CLI-vel
# Előfeltétel: telepített és bejelentkezett `codex` parancs.
# Először mindig csak egy blokkot tesztelj:
# py translate_with_codex.py "blocks\Sorozat - S01E01.eng" --block 1 --agents 1
# Utána a hiányzó blokkok fordítása:
# py translate_with_codex.py "blocks\Sorozat - S01E01.eng" --agents 1

# 3. Összefűzés (megint csak sima Python)
py merge_srt.py "blocks\Sorozat - S01E01.eng" "output\Sorozat - S01E01.hun.srt"
# Hiányzó blokkoknál leáll és listázza őket; --force kapcsolóval mégis összefűz

# 4. Ellenőrzés — ✓ vagy ✗ jelzi ha valami nem stimmel
py verify_srt.py "input\Sorozat - S01E01.eng.srt" "output\Sorozat - S01E01.hun.srt"

# 5. Stilisztikai review Claude-dal (tükörfordítások, ragozási hibák)
py review_with_claude.py "output\Sorozat - S01E01.hun.srt"
# Default modell: sonnet
# Ha megtalálja a forrásnyelvi SRT-t (input\Sorozat - S01E01.eng.srt), automatikusan
# odaadja a modellnek szekciónként [FORRÁS] sorként — így kevesebb a téves találat.
# Opcionálisan:
#   --chunk-size 150            nagyobb chunkok = kevesebb Claude hívás
#   --model haiku|sonnet|opus   modell-választás (default sonnet)
#   --source "input\....srt"    forrásnyelvi SRT kézi megadása (bármilyen nyelv;
#                               a régi név, --english, továbbra is működik)
#   --no-source                 forrásnyelvi SRT kihagyása
#   --start-chunk N             chunk-tartomány pl. megszakítás utáni pótlásra
#   --end-chunk N --suffix _part2  hogy ne írja felül a meglévő riportot
# Kimenet: output\Sorozat - S01E01.hun_REVIEW_CLAUDE.txt (olvasható riport)
#          output\Sorozat - S01E01.hun_REVIEW_CLAUDE.json (az apply_review.py-hoz)
# A javításokat az 5c. lépés (apply_review.py) viszi át interaktívan

# 5b. Stilisztikai review Gemini-vel (alternatív / kiegészítő, Te döntöd mikor futtatod)
py review_with_gemini.py "output\Sorozat - S01E01.hun.srt"
# Default modell: gemini-3.1-flash-lite
# Ha megtalálja a forrásnyelvi SRT-t (input\Sorozat - S01E01.eng.srt), automatikusan
# odaadja Gemini-nek szekciónként [FORRÁS] sorként — így kevesebb a téves találat.
# Opcionálisan:
#   --chunk-size 150            nagyobb chunkok = kevesebb API hívás
#   --model <név>               más Gemini modell (pl. gemini-3.1-flash, gemini-2.5-flash, gemini-3.1-pro-preview)
#   --source "input\....srt"    forrásnyelvi SRT kézi megadása (bármilyen nyelv;
#                               a régi név, --english, továbbra is működik)
#   --no-source                 forrásnyelvi SRT kihagyása
#   --start-chunk N             chunk-tartomány pl. kvótahiba utáni pótlásra
#   --end-chunk N --suffix _part2  hogy ne írja felül a meglévő riportot
# Kimenet: output\Sorozat - S01E01.hun_REVIEW_GEMINI.txt (olvasható riport)
#          output\Sorozat - S01E01.hun_REVIEW_GEMINI.json (az apply_review.py-hoz)
# Előfeltétel: pip install google-genai python-dotenv pydantic
# Előfeltétel: .env fájl GEMINI_API_KEY=... bejegyzéssel

# 5c. Stilisztikai review Codex CLI-vel (alternatív / kiegészítő)
# py review_with_codex.py "output\Sorozat - S01E01.hun.srt"
# Kimenet: output\Sorozat - S01E01.hun_REVIEW_CODEX.txt + .json

# 5b/2. GEMINI KVÓTA — mennyi hívás van még ma?
#     A Gemini API NEM adja vissza a maradék napi kvótát (kipróbálva: a Service
#     Usage API 403-at ad API kulccsal, a válaszfejlécekben nincs ratelimit-*),
#     ezért a gemini_quota.py HELYBEN könyveli a hívásokat.
py gemini_quota.py                 # mai fogyás modellenként
# Opcionálisan:
#   --days 7        utolsó 7 nap
#   --projects      projektmappa szerinti bontás is (hol fogyott el az adag)
#   --model <név>   csak egy modell
#   --where         hol van a napló
#   --reset         mai számlálók nullázása (ha félrement a könyvelés)
#   --forget-limit <modell>   egy megtanult napi limit elfelejtése
#                             (a limitek tartósak, a --reset nem érinti őket)
#
# A napló GÉPSZINTŰ, NEM projektszintű — ez fontos:
#   ~\.gemini_quota\usage.json   (felülírható: GEMINI_QUOTA_FILE env-változóval)
# A napi kvóta az API KULCSHOZ tartozik, nem a mappához. Ha egy gépen több
# felirat-projekt fut ugyanazzal a kulccsal, MIND ugyanabból a napi adagból
# fogyaszt — ezért közös a napló, és a kulcs ujjlenyomata szerint van bontva
# (maga a kulcs SOHA nem kerül a fájlba).
#
# A review_with_gemini.py és a translate_with_gemini.py automatikusan használja:
#   - indulás előtt kiírja, belefér-e a tervezett futás a napi limitbe
#   - minden sikeres hívást könyvel
#   - NAPI limitbe futáskor (429) kiolvassa a tényleges limitet a hibából és
#     elmenti, így onnantól arra a modellre pontos limitet ismer (nem becsül)
#
# A 429 nem mind egyforma! A Gemini percenkénti (RPM) és token-limitre is 429-et
# ad — azok MÚLÓ hibák, a scriptek retry-logikája átvészeli őket. A könyvelés
# ezért a hiba quotaId mezője alapján osztályoz, és CSAK a napi (PerDay) limitet
# tanulja meg. Enélkül egy múló percenkénti hiba (magas --agents értéknél ez
# rutinszerű) rossz napi limitet égetne be, és hamisan "kimerült"-nek jelölné
# a modellt a nap hátralévő részére.
#
# FIGYELEM: a nap NEM helyi éjfélkor vált! A Google ingyenes napi kvótája
# csendes-óceáni idő (PT) szerint nullázódik. A script PT szerint könyvel, és
# kiírja, hány óra van a nullázásig.
#
# Tapasztalat: az ingyenes szinten a nem-lite modellek napi limitje 20 kérés
# MODELLENKÉNT (a limit modellenként külön van, ezért modellváltással lehet
# tovább dolgozni). Egy ~400 szekciós rész review-ja 4-5 kérés.

# 5c. Review-javaslatok alkalmazása (a két riportot összefésüli és deduplikálja)
py apply_review.py "output\Sorozat - S01E01.hun.srt"
# Automatikusan megtalálja a fájl melletti _REVIEW_CLAUDE / _REVIEW_GEMINI / _REVIEW_CODEX
# riportokat (a .json a kanonikus, a .txt-t is érti). Találatonként kérdez:
#   y = alkalmaz, 1..9 = adott variáns, e = kézi szerkesztés, n = kihagy,
#   q = kilépés (az addig alkalmazottak mentődnek)
# Az első íráskor .bak mentés készül az eredetiről.
# Csak listázás módosítás nélkül: --dry-run

# 5d. ALTERNATÍVA: review-javítások NEM interaktív alkalmazása
#     Az 5c. lépés konzolon kérdez minden találatnál — sok részt átnézve ez
#     több száz kérdés. Ha egy agent (vagy Te, előre elkészített listával)
#     vezeti át a javításokat, ez a script kérdés nélkül dolgozik:
py apply_review_auto.py "output\Sorozat - S01E01.hun.srt" decisions.json
# A decisions.json formátuma (a sorszám + időbélyeg SOHA nem módosul):
#   [
#     {"sorszam": 12, "eredeti": "A régi szöveg", "javaslat": "Az új magyar szöveg"},
#     {"sorszam": 40, "javaslat": "Első sor\nMásodik sor"}
#   ]
# Csak listázás módosítás nélkül: --dry-run
# Az első íráskor .bak mentés készül (ha még nincs).
#
# Az "eredeti" mező OPCIONÁLIS, de AJÁNLOTT: ha megadod, a script ellenőrzi,
# hogy tényleg az áll-e a fájlban, és eltérésnél kihagyja a bejegyzést. Ez azért
# kell, mert a sorszámok nem örökérvényűek — a resegment_srt.py --split
# ÚJRASZÁMOZZA a cue-kat, és onnantól egy régebbi döntés-fájl más szekciókra
# mutat. Enélkül a script némán rossz helyekre írna. A review riportokban
# (_REVIEW_*.json) amúgy is benne van ez a mező, csak át kell másolni.
# Felülbírálás: --ignore-drift
#
# A döntés-fájl összeállítását (javaslatok forráshoz mérése, szűrés) a
# /review-triage skill végzi el Claude Code-ban — kézzel nem kell.
#
# MIÉRT ÉRDEMES ÍGY: a review-modellek javaslatainak jó része téves — mért
# adat 10 részen: a gemini-3.1-flash-lite találatainak ~13%-a szó szerint
# azonos volt az eredetivel (no-op), és ezen felül is sokat el kell dobni
# (nem létező szóalakok, hamis tegezés/magázás-riasztások). A javaslatokat
# ezért érdemes egyesével az ANGOL EREDETIHEZ mérni, és a jóváhagyott
# (esetleg átírt) szöveget ebben a fájlban átadni.

# 6. Szójegyzék bővítése a KÉSZ fordításból (fordítás UTÁNI mód)
#     Itt MINDKÉT fájlt megadod → a "hu" a magyar feliratban TÉNYLEGESEN használt fordítás.
#     (Az 1.5 lépés a fordítás ELŐTT javasol; ez a lépés UTÓLAG, a kész párból tanul.)
py glossary_extract.py "input\Sorozat - S01E01.eng.srt" "output\Sorozat - S01E01.hun.srt"
# Interaktívan jóváhagyod a javasolt kifejezéseket (y/n/e/q)
# A glossary.json-t minden provideres fordító és review automatikusan
# betölti és átadja a modellnek (system prompt-ként) a következő futáskor.

# 7. Szegmentálás — sorhossz + CPS (véglegesítő formázás, a kézi review-javítások UTÁN)
#     Csak Python stdlib, nincs telepítendő függőség. Kiegészíti a verify_srt.py-t
#     (az CPS-t jelzi, de sorhosszt/tördelést nem). Részletek: resegment_srt.md
# 7a. QA-riport (csak olvasás) — bármikor futtatható, ez a QA-kapu:
py resegment_srt.py report "output\Sorozat - S01E01.hun.srt"
# 7b. Sorhossz-tisztítás (időzítést NEM változtat) — a VÉGLEGES szövegen futtasd:
py resegment_srt.py reflow "output\Sorozat - S01E01.hun.srt" -o "output\Sorozat - S01E01.hun.reflow.srt"
# Opcionálisan:
#   --in-place                     a bemenet felülírása (új fájl helyett)
#   --split                        2 sorba nem férő cue-k idő-arányos bontása (cue-számot változtat)
#   --max-chars 42 --target-cps 17 plafonok (más csatorna = más érték)
#   --lang hu                      nyelvi töréslogika (kötőszavak, névelők)


# Tippek:
# - Először mindig egy blokkot teszteld --agents 1-gyel; ha jó, emeld 3-ra.
# - A translate script checkpoint-os: ha leáll, egyszerűen futtasd újra,
#   csak a hiányzó blokkokat fordítja.
# - A TRANSLATION.md és glossary.json változtatása automatikusan érvényesül
#   a következő futáskor (mind translate-nél, mind review-nál).
